package net.straxidus.dtm.game;

import net.straxidus.dtm.map.DTMMap;

public class RankedGame extends Game {
    public RankedGame(int id) {
        super(id);
    }
    public RankedGame(DTMMap map, int id) {
        super(map, id);
    }
}
